## movebin

A simple Zig utility to move binaries to bin for easy execution from anywhere. Usage: 

```bash
sudo movebin <binary_path> [args...]
```

Prevents overwriting unless a force flag is provided.
